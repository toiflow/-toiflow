name: must-update-timing

on:
  workflow_call:
    inputs:
      quarter_override:
        description: 'Override quarter (e.g. 2026Q3) — leave empty to use current'
        type: string
        required: false
      categories:
        description: 'Comma-separated category names for could/ files (e.g. ANALYSIS,BUG). Leave empty for CONTENT default.'
        type: string
        required: false

jobs:
  timing:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Compute quarter
        id: quarter
        run: |
          if [ -n "${{ inputs.quarter_override }}" ]; then
            QUARTER="${{ inputs.quarter_override }}"
          else
            YEAR=$(date +%Y)
            MONTH=$(date +%-m)
            Q=$(( (MONTH - 1) / 3 + 1 ))
            QUARTER="${YEAR}Q${Q}"
          fi
          echo "value=$QUARTER" >> $GITHUB_OUTPUT
          echo "Resolved quarter: $QUARTER"

      - name: Create quarterly files
        id: create
        env:
          QUARTER: ${{ steps.quarter.outputs.value }}
          CATEGORIES: ${{ inputs.categories }}
        run: |
          CREATED=false

          ASSET_HEADER="ASSET LOG
INSTRUCTION FOR AI MODEL:

ALWAYS ADD NEW ASSET ENTRIES AT THE TOP, DIRECTLY BELOW THIS HEADER.

NEVER DELETE OR EDIT PREVIOUS ASSET ENTRIES.

REQUIRED FORMAT FOR EACH ASSET ENTRY:

## ASSET:{NAME OF ENVIRONMENT} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD ALL NEW ASSET ENTRIES DIRECTLY BELOW THIS LINE, NEVER DELETE OR EDIT PREVIOUS ASSET ENTRIES-->"

          ISSUE_HEADER="ISSUE LOG
INSTRUCTION FOR AI MODEL:

ALWAYS ADD NEW ISSUE ENTRIES AT THE TOP, DIRECTLY BELOW THIS HEADER.

NEVER DELETE OR EDIT PREVIOUS ISSUE ENTRIES.

REQUIRED FORMAT FOR EACH ISSUE ENTRY:

## ISSUE:{NAME OF ENVIRONMENT} {YYYY-MM-DD HH:MM} → {CONTENT}

####### <!-- ANCHOR MARKER - ADD ALL NEW ISSUE ENTRIES DIRECTLY BELOW THIS LINE, NEVER DELETE OR EDIT PREVIOUS ISSUE ENTRIES-->"

          CSV_HEADER="date,quarter,environment,summary"

          create_file() {
            local path="$1"
            local content="$2"
            if [ ! -f "$path" ]; then
              mkdir -p "$(dirname "$path")"
              printf "%s" "$content" > "$path"
              echo "✅ Created: $path"
              CREATED=true
            else
              echo "— Exists:  $path"
            fi
          }

          # Root docs
          create_file "ASSET-${QUARTER}.md" "$ASSET_HEADER"
          create_file "ISSUE-${QUARTER}.md" "$ISSUE_HEADER"

          # could/ files — per category or default CONTENT
          if [ -n "$CATEGORIES" ]; then
            IFS=',' read -ra CATS <<< "$CATEGORIES"
            for CAT in "${CATS[@]}"; do
              CAT=$(echo "$CAT" | xargs)
              create_file "could/${CAT}-ASSET-${QUARTER}.md" "$ASSET_HEADER"
              create_file "could/${CAT}-ISSUE-${QUARTER}.md" "$ISSUE_HEADER"
            done
          else
            create_file "could/CONTENT-ASSET-${QUARTER}.md" "$ASSET_HEADER"
            create_file "could/CONTENT-ISSUE-${QUARTER}.md" "$ISSUE_HEADER"
          fi

          # would/ CSV log
          create_file "would/-log-asset-${QUARTER}.csv" "$CSV_HEADER"

          echo "created=$CREATED" >> $GITHUB_OUTPUT

      - name: Commit new files
        if: steps.create.outputs.created == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "would-update-timing: init ${{ steps.quarter.outputs.value }} files"
          git push
